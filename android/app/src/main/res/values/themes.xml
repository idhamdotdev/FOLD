<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base dark theme for the whole app -->
    <style name="Theme.FOLD" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent_dark</item>
        <item name="colorOnPrimary">@color/text_primary</item>
        <item name="colorSurface">@color/bg_surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:textColor">@color/text_primary</item>
        <!-- TextInputLayout outline color -->
        <item name="boxStrokeColor">@color/accent</item>
    </style>

    <!-- Fully immersive theme for DisplayActivity (no bars, pure black) -->
    <style name="Theme.FOLD.Display" parent="Theme.FOLD">
        <item name="android:windowBackground">#000000</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
